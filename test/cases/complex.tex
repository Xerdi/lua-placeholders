%% complex.tex -- All six permutations of {list, object, table} composed
%% three deep, rendered with payload values loaded.  See complex-body.tex
%% for the actual layout; this file is just the wrapper that loads the
%% recipe and payload into the jobname's namespace.
%%
%% complex-empty.tex is the corresponding example-only render (recipe
%% loaded, no payload), exercising the same body to confirm that a
%% document compiles cleanly when every cell falls back to its
%% placeholder --- including the inner complex columns.
\documentclass{article}
\usepackage{lua-placeholders}
\usepackage[autolanguage]{numprint}

\loadrecipe[\jobname]{complex-spec.yaml}
\loadpayload[\jobname]{complex-payload.yaml}

\begin{document}
    \input{complex-body.tex}
\end{document}
